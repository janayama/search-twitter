== README

Application created as an example of using CartoDB Api to create maps using the import functionality.

In order to run it will be needed to declare the constants of config/config.yml.

To run: rails server run.